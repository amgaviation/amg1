import { requireRole } from "@/lib/portal/session"; import { createServiceClient } from "@/lib/supabase/server"; import { DataTable } from "@/components/portal/ui/data-table"; import { PageHeader,SectionCard } from "@/components/portal/ui/primitives";
export default async function Aircraft(){await requireRole("admin");const db=await createServiceClient();const {data=[]}=await db.from("logbook_entries").select("aircraft_identifier,aircraft_type,flight_date,total_time").not("aircraft_identifier","is",null).order("flight_date",{ascending:false}).limit(1000);const map=new Map<string,any>();for(const x of data as any[]){const v=map.get(x.aircraft_identifier)??{id:x.aircraft_identifier,identifier:x.aircraft_identifier,type:x.aircraft_type,flights:0,time:0,last:x.flight_date};v.flights++;v.time+=Number(x.total_time??0);if(x.flight_date>v.last)v.last=x.flight_date;map.set(x.aircraft_identifier,v);}return <><PageHeader eyebrow="Flight Intelligence" title="Aircraft" description="Identifiers imported from ForeFlight. Map to AMG aircraft only after human verification."/><SectionCard title="Imported identifiers" icon="plane"><DataTable rows={[...map.values()]} getKey={(x:any)=>x.id} emptyLabel="No aircraft identifiers imported." columns={[{header:"Identifier",cell:(x:any)=>x.identifier},{header:"Type",cell:(x:any)=>x.type??"-"},{header:"Flights",cell:(x:any)=>x.flights},{header:"Total time",cell:(x:any)=>x.time.toFixed(1)},{header:"Latest",cell:(x:any)=>x.last}]}/></SectionCard></>}
